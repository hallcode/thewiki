<section data-layout="normal" class="{{ _color }}">
    <header>{{ header }}</header>
    {{# keys }}
        <div class="key">{{ key }}</div>
        <div class="value">{{ value }}</div>
    {{/keys}}
</section>